{
  "name": "forge-ecs-platform",
  "version": "1.0.0",
  "description": "FORGE Platform — Cost-optimized AWS ECS deployment",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy --all --require-approval never",
    "destroy": "cdk destroy --all --force",
    "diff": "cdk diff",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0",
    "typescript": "~5.6.0",
    "ts-node": "^10.9.2",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.14",
    "ts-jest": "^29.2.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.0"
  }
}
